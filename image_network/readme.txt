User need to have 2 RggBers to build this demo. 

The project in HDMI_CIS_colorbar_1920x1080 folder need to downloaded to the RggBer which controlled by App.

And the project in HDMI_CIS_1920x1080 folder need to downloaded to the other RggBer. 